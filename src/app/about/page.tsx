import { AboutPage } from '@/components/AboutPage'

export const metadata = {
  title: 'About',
  description: 'About Martin Zhao — the author behind Silver Bullet.',
}

export default function Page() {
  return <AboutPage />
}
