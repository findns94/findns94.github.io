name = "findns94-github-io"
main = "src/index.ts"
compatibility_date = "2026-09-06"

# Route this worker to your domain
# After running `wrangler deploy`, configure the route in Cloudflare dashboard:
#   Route: findns.cc/*
#   Worker: markdown-negotiation
